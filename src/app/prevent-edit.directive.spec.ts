import { PreventEditDirective } from './prevent-edit.directive';

describe('PreventEditDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventEditDirective();
    expect(directive).toBeTruthy();
  });
});
