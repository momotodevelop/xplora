import { IataSubstitutionPipe } from './iata-substitution.pipe';

describe('IataSubstitutionPipe', () => {
  it('create an instance', () => {
    const pipe = new IataSubstitutionPipe();
    expect(pipe).toBeTruthy();
  });
});
