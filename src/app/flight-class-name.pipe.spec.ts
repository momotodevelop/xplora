import { FlightClassNamePipe } from './flight-class-name.pipe';

describe('FlightClassNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FlightClassNamePipe();
    expect(pipe).toBeTruthy();
  });
});
