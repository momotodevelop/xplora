import { BoardTypePipePipe } from './board-type-pipe.pipe';

describe('BoardTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BoardTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
